# HANDOFF to another Claude — 2026-08-17

**Situation:** prior Claude session shipped a bunch of reliability work (R1-R5, R3, gate, lock, phone precondition, R7, SOAK harness, PHASE1-5 plan docs). User just did the FIRST live test call on the new base branch. Response time regressed from ~3s to ~7-9s wall-clock on "hello can you hear me". Voice sounds choppy on Sarah. Handing off because prior Claude was stalling on the investigation and user wants a fresh eye.

**Read order:** this file → `PROJECT-LAUNCH-CHECKLIST.md` → `docs/rnd-2026-08/58-status-and-phase-map-2026-08-14.md` → the specific per-call log below.

---

## Immediate problem to solve

**Test call:** `CA99c1dc9327602d6e2062e497dce25834`
**Per-call log:** `apps/api/data/logs/calls/CA99c1dc9327602d6e2062e497dce25834.log`
**Uvicorn log:** `apps/api/data/logs/uvicorn-2026-08-17_181506.log`

**Symptom (user's words):** "voice is kinda breaky and we have went back to the 9 second mark to respond to 'hello can you hear me'. It used to be just 3 seconds. And I think it was double responding."

**Observed in log:**
- greeting ends 18:16:37,779 → LISTENING
- STT commits final "Hello? Can you hear me?" at 18:16:43,046 (speculative brain fires)
- ⚠️ `EVENT_LOOP_LAG lag_ms=260.5` at 18:16:42,656 (just before STT commit)
- `COMMIT_LOCK_CLAIM gen=0 reason=speculative` then `COMMIT_LOCK_SKIP gen=0 reason=run_brain (slot already claimed)` at 18:16:43,053 — one-gen-lock working as designed
- `filler firing on slow brain` at 18:16:45,105 — brain is >2s slow
- `TTS_STREAM_START gen=1 text='Yep, I can hear you! What can I help you with today?'` at 18:16:45,580
- `TTS_FIRST_BYTE first_byte_ms=295` at 18:16:45,875
- **NO `CONV_CONTROL_FASTPATH_HIT` line** — the deterministic fastpath for "hello can you hear me" did NOT match. That's the primary regression suspect.
- **NO `LLM_CALL` / `LLM_STREAM_START` line** — yet a reply spoke. Something served it without hitting an LLM AND without hitting the conv-control fastpath. Might be the response cache (also no log line) OR the persistent OpenAI WS (should log `openai_ws`).
- `ZOMBIE_SPEAKING call=... idle_s=17.1 — forcing SPEAKING→LISTENING` at 18:17:06 on gen=1 — R1 watchdog fired (working) but proves state got stuck for 17s on gen=1

**Also:** TwiML `<Parameter>` variables coming through as literal `{{From}}`/`{{To}}` — Twilio isn't expanding them. See line `caller='{{From}}' dialed='{{To}}'`. R3 phase 3 ANI resolver has zero real data because of this.

## Suspects for the latency regression (in order)

1. **`_try_conversation_control_fastpath` isn't matching "Hello? Can you hear me?"** — the intent matcher in `packages/voice/conversation_control.py` used to match this; something (recent R3/gate/lock/arrival changes?) broke the string comparison or the fastpath is bypassed. Check: `grep -n "match_conversation_control_intent" packages/voice/conversation_control.py` and dry-run the matcher on the exact string `"Hello? Can you hear me?"`.

2. **smart-turn-v3 ONNX loaded at startup** (see uvicorn log `smart-turn-v3 loaded model=smart-turn-v3.2-cpu.onnx`). If it runs per-turn CPU inference on the receive loop, that explains the 260ms event-loop-lag. Check if this is new since the last known-fast baseline.

3. **Cold TTS cache for the reply text.** Sarah voice_id `EXAVITQu4vr4xnSDxMaL` is fresh — no prior cached utterances. First-call TTS goes to ElevenLabs (~800ms first byte). But log shows first_byte_ms=295, so this isn't the main hit — it's contributory only.

4. **Persistent OpenAI WS scaffold** (`openai_persistent_ws_enabled`) — may or may not be enabled; if it is and it's failing to connect but not falling back cleanly, could add latency. Verify with `grep -n "openai_persistent_ws_enabled" apps/api/app/core/config.py` and check .env.

## What the code state looks like

**Base branch:** `feat/architectural-networking` @ `bf63cf7`
**Recent commits (see `git log --oneline`):**
```
bf63cf7 docs: launch checklist points to new playbook + tooling plan + scale reference
88346ca docs: per-branch validation playbook + SOAK tooling plan
5dc9ac6 chore: prune merged branches + update launch checklist
f863d5a docs: PROJECT-LAUNCH-CHECKLIST.md — zero-orientation restart
edacdec merge: SOAK test harness
7888385 merge: R7 telephony-arrival observability
14d1075 docs(plans): PHASE 1-5 implementation plans
5816793 feat(R3 phase-4 slim v1): phone precondition on book_appointment
b9a3fa1 feat(R3 phase-3): DTMF feed + ANI resolver
25cba53 fix(task-#369): one-generation-one-commit invariant
817df97 feat(SpeechCommitGate): deterministic pre-TTS gate for streaming replies
e1f860e feat(R3 phase-2): wire StructuredInputSession into TwilioActorSession
```

**Held branches (do NOT merge — they're isolated for a reason):**
- `feat/r6-prebuffer-backpressure` @ `36e9652` — AudioPipeline module, not wired
- `feat/phase2-kernel-wire` @ `6ffaa70` — kernel shadow observer, not wired

**Test suite baseline:** 1171 passed, 19 pre-existing failed (documented in checklist)

## The greater plan — where the doc trail lives

**Read in order:**
1. **`PROJECT-LAUNCH-CHECKLIST.md`** (repo root) — 3-command restart, phase 0 orientation
2. **`docs/rnd-2026-08/58-status-and-phase-map-2026-08-14.md`** — current authoritative index, what shipped, phase map
3. **`docs/rnd-2026-08/59-phase0-validation-plan.md`** — the 5-box sanity gate (all still open)
4. **`docs/rnd-2026-08/56-phase-change-my-analysis.md`** — originating analysis + Addenda A-D (ratified adds like SpeechCommitGate that MUST survive future refactors)

**Then depending on what you're doing:**
- Building/testing a branch → `docs/superpowers/plans/2026-08-15-branch-validation-playbook.md` (the 5-step per-branch loop)
- Latency work → `docs/superpowers/plans/2026-08-14-phase1-latency-lab.md`
- Kernel work → `docs/superpowers/plans/2026-08-14-phase2-2a-shadow.md` through `-2d-retire.md`
- RAG → `docs/superpowers/plans/2026-08-14-phase3-rag-2.md`
- Commercial → `docs/superpowers/plans/2026-08-14-phase4-4a-booking.md` / `-4b-crm.md` / `-4c-followups.md`
- Differentiators → `docs/superpowers/plans/2026-08-14-phase5-differentiators.md`
- Scale question → `docs/rnd-2026-08/61-scale-tiers-reference.md`

## The 3 ratified adds (from doc #56 Addendum B) — do NOT re-invent

1. **SpeechCommitGate** `packages/core_agent/speech_commit_gate.py` — deterministic pre-TTS classifier
2. **One-generation-one-commit lock** in `apps/api/app/routes/twilio_actor.py` — `_try_claim_response_commit` / `_clear_response_commits_before`
3. **Phone precondition** `packages/integrations/clinic_tools.py` — libphonenumber pre-write validation

## What the user actually wants next

They want the latency regression fixed. They said "it used to respond FAST before" and rejected my cache-guess. Real root cause is likely one of the 4 suspects listed above. Investigate empirically — do NOT ship speculative fixes.

**Suggested first action:** run this in the repo to compare good-old vs today's behavior on `_try_conversation_control_fastpath`:
```bash
cd "/Users/az/Desktop/Receptionist Agent/apps/api"
python -c "
import sys
sys.path.insert(0, '.'); sys.path.insert(0, '../..')
from packages.voice import match_conversation_control_intent
inputs = ['Hello? Can you hear me?', 'hello can you hear me',
          'Hello can you hear me?', 'hello?']
for i in inputs:
    r = match_conversation_control_intent(i)
    print(f'{i!r:<40} -> {r!r}')
"
```

If any of those return None, the intent matcher regressed. That's the 3s → 9s. Fix by walking the matcher's tokenization logic; last-known-working version was pre-R3.

## What's in this zip

- Full codebase (excludes `.venv`, `.env`, secrets, cached models, audio dumps, node_modules, git history, uvicorn logs)
- All 25 rnd-2026-08 docs (research + phase map)
- All 17 superpowers/plans docs (implementation plans)
- 4 per-call logs including THE problematic one `CA99c1dc9327602d6e2062e497dce25834.log`
- SOAK harness (`docs/soak/`, `apps/api/scripts/verify-call.sh`, `apps/api/scripts/replay-audio.py`)
- PROJECT-LAUNCH-CHECKLIST.md
- This HANDOFF-2026-08-17.md
- ADDED: uvicorn log for the problematic call (`apps/api/data/logs/uvicorn-2026-08-17_181506.log`) — shows the boot sequence + smart-turn-v3 load + filler pool warm + greeting cache warm + all the 260ms lag warnings

Good luck. The plan docs are thorough; the regression is empirical. Investigate the log, not the plans.
